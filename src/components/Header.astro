---
import NavLink from '@components/ui/links/NavLink.astro'
import BrandLogo from '@components/ui/logos/BrandLogo.astro'
import { NavigationLinks } from '@/config'
import logoSrc from '@images/concordance-logo.svg'
import Image from '@components/ui/images/Image.astro'
import { ContactButton } from '@/components/ContactButton'

---
<header class="flex items-center max-w-screen-xl mx-auto w-full px-0">
  <a href="/" class="flex items-center gap-6">
    <!-- <Image disableBlur src={logoSrc} alt="logo" class="w-[50px] md:w-[50px] lg:w-[50px] xl:w-[45px] h-auto" />
    <div class="font-amstelvar-lead md:block">
      Concordance
    </div> -->
  </a>


  <div class="grow" />


  <div class="font-amstelvar-bold-body flex gap-6 mx-auto">
    {
      NavigationLinks.map((link) => (
        <NavLink url={link.url} name={link.name} />
      ))
    }
  </div>
</header>
